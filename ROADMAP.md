# ROADMAP — 后续任务与计划（v2 三方聚合版）

> 来源：MOA 多模型审视 —— GLM-5.2 + Kimi K3-256K + GPT-5.6 Luna 三份独立意见（同题异构，双维度 A旅程/B架构）→ DeepSeek V4 Flash 聚合对比
> 日期：2026-08-08（v2 更新：加入 Luna 意见，共识度标注 X/3）

## 聚合结论

### 共识项（≥2/3 家独立提出 = 最重要）
- **邀请转化断点（3/3）**：游客看不到活动内容 → 流失
- **核心抽签的测试与幂等（3/3）**：0 测试、无事务保护，改一次怕一次
- **仪式感（3/3）**：拆礼物前隐藏送礼人 + "撕开信封"动画
- **分享海报（3/3）**：Canvas 邀请海报 / 完结高光海报
- **复购钩子（3/3）**："再开一局" + 礼物档案
- **通知抽象 + 截止提醒（3/3）**：`notify(user,type,payload)` 为微信订阅消息铺路
- **工程拆分（3/3）**：Blueprint 拆分 + 对象存储抽象 + 最小测试 CI

### 独到项（单一来源但很有价值）
| 独到项 | 来源 |
|---|---|
| 晒图隐私降门槛（文字/模糊/仅参与者可见），礼物墙不因单人缺图全锁 | Luna |
| 上传安全：MIME/大小限制、日志脱敏、JWT 刷新/撤销 | Luna |
| 送礼状态机（待购买/已发货/已签收/待晒图） | Luna |
| 抽签无合法环边界（互避可能无解，限重试+明确错误码） | Kimi |
| SQLite WAL + busy_timeout（防公网体验期 database is locked） | Kimi |
| KDNiao 调用隔离（3s 超时/6h 缓存/静默降级） | Kimi |
| 礼物画像（节日"最佳送礼王/品味挑剔者"） | GLM |

### 分歧项（已裁决）
- **迁移管理**：2/3 支持 Alembic，1/3（Kimi）认为 SQL 脚本够用 → **采纳 Alembic baseline**（双引擎手写加列已出过错）
- **游客处理**：只读预览 vs 草稿绑号 → **先预览，草稿后置**（两家共识）
- **service 层**：暂不引入，draw/notify 单独抽函数即可

## 第一批（本周可做，高价值低成本）

| # | 任务 | 为什么 | 工作量 | 共识 |
|---|---|---|---|---|
| 1 | 邀请页游客预览 + 登录回跳（`invite_token` 续程） | 最大流失点 | S | 3/3 |
| 2 | 抽签逻辑抽纯函数 + pytest（防自抽/互避/无解） | 核心算法 0 测试 | S-M | 3/3 |
| 3 | 信封揭晓动效：礼物墙遮罩+Flip，悄悄话揭晓前不可见 | 仪式感，低成本高情绪价值 | S-M | 3/3 |
| 4 | 抽签幂等：事务+唯一约束+幂等键；互避无解明确错误码 | 防并发重复/挂死 | M | 2/3 |
| 5 | `notify(user,type,payload)` 抽象 + cron 每日扫截止前 48h/24h 提醒 | 用户离线无触达，为微信铺路 | M | 3/3 |
| 6 | SQLite WAL + busy_timeout | 多人写锁库 | S | 1/3 |

## 第二批（近期，中等投入）

| # | 任务 | 为什么 | 工作量 | 共识 |
|---|---|---|---|---|
| 1 | Blueprint 拆分：auth/events/participant/draw/gifts/notifications | 可维护性，微信接入前提 | M | 3/3 |
| 2 | 对象存储抽象 `storage.save`，本地实现，后续 OSS/COS 预签名 | 图片本地存储有丢失风险 | M | 3/3 |
| 3 | Canvas 分享海报：邀请海报 + 完结高光海报 | 传播闭环 | M | 3/3 |
| 4 | 晒图不阻塞：文字/模糊/仅参与者可见；缺图仍推进 | 隐私顾虑导致完成率低 | M | 1/3 |
| 5 | Alembic 迁移 baseline + 双引擎迁移脚本 | 手动加列易错 | S-M | 2/3 |
| 6 | KDNiao 封装：3s 超时/6h 缓存/降级，key 走环境变量 | 外网同步阻塞拖垮请求 | S | 1/3 |
| 7 | 送礼状态机 + 物流异常手动更新 | 减少送礼者焦虑 | S-M | 1/3 |
| 8 | 上传安全加固：MIME/大小/预签名/日志脱敏/JWT 短时效 | 地址和照片是高敏感面 | M | 1/3 |

## 第三批（远期/微信生态/规模化）

| # | 任务 | 为什么 | 工作量 | 共识 |
|---|---|---|---|---|
| 1 | 微信订阅消息：节点授权 + 异步发送队列 | 回流核心，小程序必需 | M | 3/3 |
| 2 | 小程序登录/API 收口：openid 只作 adapter、统一错误码/分页/幂等、`/v1` | 降低迁移风险 | L | 2/3 |
| 3 | 礼物档案与复购：历史墙、节日画像/模板、"再开一局" | 留存与二次发起 | L | 3/3 |
| 4 | 可观测性：结构化日志、request_id、漏斗埋点、告警 | 定位邀请/抽签/通知失败 | S | 1/3 |

## 总结判断（聚合器）

这个产品现在最该把力气花在**"邀请转化"和"核心抽签的测试/幂等"**上：前者决定增长，后者决定稳定。紧接着做 **Blueprint 拆分和对象存储抽象**，让后端可以被安全地继续扩展。微信迁移只需守住接口边界，不必现在过度设计。
